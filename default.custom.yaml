patch:
  schema_list:
    - schema: rime_ice
    - schema: double_pinyin_flypy

  ascii_composer:
    switch_key:
      Shift_L: inline_ascii

  # key_binder:
  #   bindings:
  #     - when: always
  #       accept: Shift+Space
  #       toggle: ascii_mode 
