patch:
  schema_list:
    # pinyin
    - schema: rime_ice
    # flypy double pinyin
    - schema: double_pinyin_flypy

  ascii_composer:
    # by which to switch eng/zh
    switch_key:
      # Shift_L: inline_ascii
      Shift_L: commit_code
      # Shift_R: commit_code

  # key_binder:
  #   bindings:
  #     - when: always
  #       accept: Shift+Space
  #       toggle: ascii_mode 
