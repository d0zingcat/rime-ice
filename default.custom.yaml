patch:
  schema_list:
    # pinyin
    - schema: rime_ice
    # flypy double pinyin
    - schema: double_pinyin_flypy

  # by which to switch eng/zh
  ascii_composer/switch_key:
      Caps_Lock: noop
      # Shift_L: inline_ascii
      Shift_L: commit_code
      Shift_R: noop
      Control_L: noop
      Control_R: noop

  # key_binder:
  #   bindings:
  #     - when: always
  #       accept: Shift+Space
  #       toggle: ascii_mode 
